[package]
name = "afs"
version = "0.1.3"
edition = "2024"
repository = "https://github.com/InterstelarDev/auto_file_sorter"

[dependencies]

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
